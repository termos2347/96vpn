# 96VPN Bot

Telegram-бот для продажи VPN-подписок с интеграцией 3x‑UI, ЮKassa и Telegram Stars.

## Возможности
- VPN-подписки через пул серверов 3x‑UI.
- Оплата через ЮKassa (RUB), Telegram Stars, USDT (через ссылку).
- Автоматическое создание/отзыв ключей.
- Напоминания об истечении, фоновые задачи.
- Административный бот для управления пользователями, серверами, рассылками.

## Технологии
- aiogram 3.x
- aiohttp (внутреннее API)
- PostgreSQL, SQLAlchemy, Alembic
- Yookassa SDK, cryptography, tenacity

## Установка
1. Клонируйте репозиторий.
2. Создайте виртуальное окружение и установите зависимости (`pip install -r requirements.txt`).
3. Скопируйте `.env.example` в `.env` и заполните обязательные переменные.
4. Примените миграции: `alembic upgrade head`.
5. Запустите: `python run_all.py`.
```
git clone https://github.com/your-repo/96vpn-bot.git
cd 96vpn-bot
python3 -m venv .venv
source .venv/bin/activate          # для Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

## Запуск
```bash
npx localtunnel --port 5001 --subdomain my-test-96
```

```bash
chmod +x run.sh
./run.sh
```
## Миграции базы данных

Миграции **не применяются автоматически** при запуске бота.

Для применения миграций выполните вручную одну из команд (из корня проекта):

```bash
# через Alembic напрямую
alembic upgrade head

# если создан скрипт-обёртка
python migrate.py
```

## Команды бота
- Основной бот: кнопки в меню.
- Админ-бот: `/health`, `/grant`, `/revoke`, `/stats`, `/broadcast` и т.д.

## Внутреннее API
- `/activate` – активация подписки (защищено токеном).
- `/yookassa_webhook` – вебхук для ЮKassa.
- `/webhook` – вебхук основного бота.
- `/webhook/admin` – вебхук админ-бота.

## Архитектурные особености
- Бот работает **только в одном экземпляре** (однопроцессный).
- **Rate limiting** и **кэш** хранятся в оперативной памяти и сбрасываются при перезапуске бота.
- Для горизонтального масштабирования (несколько воркеров) потребуется внешнее хранилище (Redis, Memcached) и централизованное управление сессиями.
- В текущей версии Redis **не используется** и не поддерживается.

### Для кого подходит текущая версия?

- Для старта проекта с нагрузкой до нескольких тысяч пользователей.
- Для тестирования и разработки.
- Для случаев, когда не требуется высокий уровень отказоустойчивости и масштабирования.

## 🌐 Архитектура и Масштабирование (Master-Node)

Бот спроектирован по облегченной и максимально отказоустойчивой схеме. В коде бота **полностью отсутствует** сложная логика балансировки серверов и ручного распределения нагрузки.

### Как это работает:
1. **Бот + Master-панель:** Бот связан через API строго с **одной главной панелью 3X-UI (Master-сервер)**. Все запросы на создание, продление или удаление VPN-ключей идут исключительно на этот Master-URL.
2. **Удаленные ноды (Nodes):** Масштабирование сети и добавление новых локаций (стран/серверов) происходит напрямую через веб-интерфейс Master-панели 3X-UI. Новые сервера подключаются как подчиненные ноды.
3. **Распределение нагрузки:** Панель 3X-UI сама синхронизирует базу пользователей и автоматически распределяет трафик между подключенными нодами.

### 🚀 Плюсы такого подхода:
* **Код бота не перегружен** лишними таблицами серверов, пингами и алгоритмами балансировки.
* Для добавления 10, 100 или 1000 новых VPN-серверов **не требуется изменять код бота или перезапускать его**. 
* Стабильность системы упирается только в мощность Master-сервера, а трафик клиентов идет напрямую через ноды, не нагружая бота.
